\documentclass[11pt, letterpaper]{article}
\input{../../homework.tex}
\usepackage{titlepageBU}
\title{Homework 2}
\courseID{MA 564}
\courseSection{A1}
\DeclareMathOperator{\Int}{Int}
\DeclareMathOperator{\Graph}{Graph}
\begin{document}
\makeproblem
\section*{Problem 1}
\section*{Problem 2}
\section*{Problem 3}
\[
	\partial A\coloneqq \overline{A} \setminus \Int(A)
.\]
Let $A$ be closed. Then $A=\overline{A} $. Note that $\overline{A} \setminus \Int(A) \subseteq \overline{A} $, and $\partial A=\overline{A} \setminus \Int(A) $. Therefore, $\partial A\subseteq \overline{A} =A$.

Now for the converse, let $\partial A\subseteq A$. It follows that $\overline{A} \setminus \Int(A) \subseteq A$. Note that $\overline{A} =\Int(A)\cup (\overline{A} \setminus \Int(A))$. Since $\Int(A)\subseteq A$ and $\overline{A} \setminus \Int(A)\subseteq A$, we have that their union must also be a subset of $A$. That is,
\[
	\overline{A} =\Int(A)\cup \left( \overline{A} \setminus \Int(A) \right) \subseteq A
.\]
Since we also know for all sets that $A\subseteq \overline{A} $, we have that $A=\overline{A} $, and $A$ is closed.
\section*{Problem 4}
\section*{Problem 5}
Let $f : X \to Y$ be continuous, and let $Y$ be Hausdorff. 
\section*{Problem 6}
\section*{Problem 7}
\end{document}

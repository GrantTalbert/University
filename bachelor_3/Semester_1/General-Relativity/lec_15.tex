\lecture{15}{Tue 04 Nov 2025 12:37}{Blacker holes}

Can we find the geodesics of the Schwartzschild metric? These should correspond to stable orbits. Recalling
\[
	\mathrm{d} s^2 = -\left( 1-\frac{2G_M}{r}  \right) \mathrm{d} t^2+\frac{1}{1-\frac{2G_M}{r} } \mathrm{d} r^2+r^2 \left( \mathrm{d} \theta ^2+ \sin ^2 \mathrm{d} \phi ^2 \right) 
,\]
write $f(r)=1-\frac{2G_M}{r} $, and use the action principle
\[
	L=\frac{1}{2} \int -g_{\mu \nu } \frac{\mathrm{d}x^\mu }{\mathrm{d}\lambda } \frac{\mathrm{d}x^\nu }{\mathrm{d}\lambda }  \,\mathrm{d} \lambda =\frac{1}{2} \int f(r)(t')^2-\frac{(r')^2}{f(r)} +r^2((\theta ')^2+\sin ^2\theta (\phi ')^2 \,\mathrm{d} \lambda 
.\]
We can choose $\theta =\pi /2$ without loss of generality (orbit in $xy$-plane). I zoned out but then
\[
	\varepsilon =-g_{\mu \nu } P^\mu P^\nu ,\qquad P^\mu =\frac{\mathrm{d}x^\mu }{\mathrm{d}\lambda } 
.\]
Then
\[
	\frac{\mathrm{d}\varepsilon }{\mathrm{d}\lambda } =P^\alpha \nabla _\alpha \left( -g_{\mu \nu } P^\mu P^\nu  \right) =0
.\]
Then if $\varepsilon >0$ we have massive particles, if $\varepsilon =0$ we have massless particles, and for $\varepsilon <0$ we have the power of love and friendship. Then you haver)
\[
	\frac{\mathrm{d}t}{\mathrm{d}\lambda } \equiv \frac{E}{f(r)} 
.\]
I dont know bbut somehow
\[
	\varepsilon =\frac{E^2-(r')^2}{f'(r)} -\frac{\ell ^2}{r^2} 
.\]
Then we get
\[
	\frac{1}{2} (r')^2+V(r)=\frac{1}{2} E^2,\qquad V(r)=\frac{1}{2} f(r)\left( \frac{\ell ^2}{r^2} +\varepsilon  \right) 
.\]
Idk but yeah. Check prof's notes.
